export const examples = {
    1: {
      
    },
    2: {
        tryItYourselfCode : [
            `contract VoteTwice =
  entrypoint voteTwice(v : VotingType, alt : string) =
    v.vote(alt)
    v.vote(alt)`
        ]
    }
}