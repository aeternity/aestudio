import { MdwUrlDirective } from './mdw-url.directive';

describe('MdwUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new MdwUrlDirective();
    expect(directive).toBeTruthy();
  });
});
