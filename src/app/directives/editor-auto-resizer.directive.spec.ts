import { EditorAutoResizerDirective } from './editor-auto-resizer.directive';

describe('EditorAutoResizerDirective', () => {
  it('should create an instance', () => {
    const directive = new EditorAutoResizerDirective();
    expect(directive).toBeTruthy();
  });
});
