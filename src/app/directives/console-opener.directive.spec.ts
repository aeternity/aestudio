import { ConsoleOpenerDirective } from './console-opener.directive';

describe('ConsoleOpenerDirective', () => {
  it('should create an instance', () => {
    const directive = new ConsoleOpenerDirective();
    expect(directive).toBeTruthy();
  });
});
